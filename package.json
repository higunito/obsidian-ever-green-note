{
	"name": "obsidian-ever-green-note",
	"private": true,
	"packageManager": "pnpm@11.12.0",
	"scripts": {
		"dev": "pnpm --filter web dev",
		"build": "pnpm -r --if-present run build",
		"lint": "biome check .",
		"typecheck": "pnpm -r --if-present run typecheck",
		"test": "pnpm -r --if-present run test"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.3"
	}
}
